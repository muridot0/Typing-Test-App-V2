function Timer({countDown}){
    return (
        <div className="section">
            <div className="is-size-1 has-text-primary">
                <h2 className="has-text-centered">{countDown}</h2>
            </div>
        </div>
    )
   }
   
   export default Timer